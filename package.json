{
  "name": "conditional-tokens-tutorial",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@gnosis.pm/conditional-tokens-contracts": "^1.0.1",
    "@gnosis.pm/conditional-tokens-market-makers": "^1.6.0",
    "chai": "^4.2.0",
    "chai-as-promised": "^7.1.1",
    "dotenv": "^8.2.0"
  },
  "scripts": {
    "test:dev": "truffle test ./test/* --network development",
    "migrate:dev": "truffle migrate --network development",
    "start": "react-app-rewired start",
    "build": "react-app-rewired build",
    "test": "react-app-rewired test",
    "eject": "react-scripts eject",
    "prettier": "prettier --write './src/**/*.{ts,tsx}'",
    "prettier-check": "prettier --check './src/**/*.{ts,tsx}'"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
